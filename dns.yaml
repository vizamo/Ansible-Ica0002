---
  - name: Init
    hosts: all
    become: yes
    gather_facts: yes
    tasks:
      - setup:
    roles:
      - init
      
  - name: DNS Server
    hosts: dns_servers
    become: yes
    roles:
      - bind

  - name: DNS Conf Setup
    hosts: all
    become: yes
    roles:
      - dns
